# SistemaIngressosOnline
Uma aplicação distribuída para cadastro de eventos, compra de ingressos e processamento de pagamentos. A solução foi construída com arquitetura de microsserviços, orquestrada via Docker Compose, com um API Gateway (Nginx) como ponto de entrada único.
